AI_PROVIDER=mock
REPORT_CHAT_PROVIDER=mock
GEMINI_API_KEY=
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/aurora_skinsense?schema=public"
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:3000
DEV_AUTH_BYPASS=false

# Set DEV_AUTH_BYPASS=true only in local development to test protected scan
# routes before authentication is connected. Never enable this in production.
