export const DEFAULT_IMAGE_RETENTION_ENABLED = false
export const DEFAULT_REPORT_RETENTION_ENABLED = true

// Uploaded images are not stored by default.
// Reports are stored for user access and admin analytics.
// Image retention requires explicit opt-in consent.
